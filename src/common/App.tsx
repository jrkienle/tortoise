import React from 'react';

import RemoteDesktop from './RemoteDesktop';

const App: React.FC = (): React.ReactElement => {
  return (
    <div>
      <RemoteDesktop />
    </div>
  );
};

export default App;
